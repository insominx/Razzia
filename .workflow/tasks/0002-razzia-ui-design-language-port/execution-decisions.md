# Execution Decisions

Last Edited: 2026-07-31

Materiality: include a choice only when a reasonable alternative could change correctness, architecture,
contract, acceptance, authority, risk, reversibility, or future maintenance. Exclude mechanical edits and
routine local coding choices.

| id | when | category | trigger | decision | alternatives | evidence / rationale | confidence | impact / reversibility | plan relationship | review disposition / provenance |
|---|---|---|---|---|---|---|---|---|---|---|
| D10 | Slice 0 / 2026-07-31 | plan deviation / tradeoff | On Windows, 134 tracked text files were checked out as CRLF while oxlint requires LF, so `pnpm lint` failed before reaching code findings. | Add repo-owned `.gitattributes` with `* text=auto eol=lf`; normalize the working tree mechanically. | Keep a machine-local `core.autocrlf=false` override; relax/remove `linebreak-style`; accept a Windows-only failing gate. | The index already stores LF (`git ls-files --eol` showed `i/lf`), and lint passed after LF normalization. A repo rule preserves the existing contract across clones without weakening lint or requiring undocumented workstation setup. | high | Low and reversible: one repository policy file; existing text content is unchanged because the index was already LF. | extends | pending |
| D11 | Slice 1 / 2026-07-31 | underspecified choice | The Canvas source specifies no teal border/tint, no danger row, and no answer-identity triples, but the Razzia role contract requires complete accent/border/tint rows in both dialects. | Derive studio teal as `#0d9488 / #99f6e4 / #f0fdfa`, danger as `#b91c1c / #fca5a5 / #fef2f2`, and answer borders/tints from the matching Tailwind color families; keep the four Okabe–Ito accents unchanged. | Reuse the structural gray border for missing rows; copy dark borders into studio; omit incomplete triples. | Same-family border/tint values preserve semantic cohesion and the full-triple invariant. The danger main is plan-locked; the other derived values are documented as inferred in `packages/web/STYLE.md`. | medium | Centralized and reversible in `index.css`; no persisted or public API impact. | aligned | pending |
| D12 | Slice 1 / 2026-07-31 | plan deviation / tradeoff | The planned `--radius-sm/md/lg/xl` target names collide with Tailwind's legacy radius variables; redefining them in the additive spine would visibly restyle existing routes before their conversion checkpoint. | Expose the target 6/10/16/20px scale as `--rz-radius-*` mapped to `rounded-rz-*` utilities until migration completes. | Override Tailwind's global radius variables now; postpone radius tokens entirely; keep Tailwind's current scale. | Slice 1 promises no visible change to existing routes. Namespaced utilities make the target available without silently changing every current `rounded-*` consumer. | high | Low-risk compatibility alias; final cleanup can rename after the last legacy consumer is gone. | deviates | pending |
| D13 | Slice 3 / 2026-07-31 | evidence correction | The plan names a numeric “Question timer,” but `states/Question.tsx` renders only a CSS progress bar; the actual visible countdown and response count live in `states/Answers.tsx`. | Apply JetBrains Mono to the visible `Answers` HUD numerals, plus the other named counter/code/score sites; do not add a meaningless font class to the non-text progress bar. | Put `font-mono` on the empty progress-bar element; add a new numeric timer to `Question`; leave the live countdown in the body face. | Repository inspection shows `Question` has no numeral node, while `Answers` renders `{cooldown}` and `{totalAnswer}/{totalPlayer}`. This preserves behavior and fulfills the typography intent at the actual owner. | high | Small class-only correction and fully reversible. | deviates | pending |
| D14 | Slice 4 / 2026-07-31 | verification strengthening | The plan names manual DevTools sampling beneath three text runs, which is placement-dependent and difficult to reproduce across 16 composites. | Compute WCAG contrast at every composited pixel for each text role and record the minimum ratio and responsible RGB pixel. | Sample only beneath current text boxes with DevTools; use mean image luminance; rely on visual inspection. | Whole-frame minima strictly contain every possible text-run sample and are deterministic. Mean luminance was rejected because a single bright pixel beneath a glyph is the failure mode. The table and fixtures are durable artifacts. | high | Evidence-method change only; no runtime effect and easy to independently reproduce. | deviates | pending |
| D15 | Slice 4 / 2026-07-31 | user-owned gate resolution | X1 found 0.75 as the only tested opacity that passes every text role on all four fixtures, but G4 also requires projected-1080p recognisability. | Accept 0.75 after the user reviewed the four full-resolution candidates and said they look okay; apply it as the runtime scrim and retire exactly the four X1-covered shadows. | Reject 0.75 and constrain uploads or the dark play language; continue without resolving G4. | The user explicitly approved the 0.75 review set. Whole-frame minimum contrast is 8.02:1, and the principal subject/dominant colour rubric is therefore also satisfied. | high | Central token change plus four reversible class removals; enables Slices 5–6. | aligned | user-approved |
| D16 | Slice 5 / 2026-07-31 | architecture / scope | The planned primitive extraction was conditional on finding a recipe repeated at least three times across converted stage and studio screens. | Do not create `src/components/ds/*`; finish Slice 6 through the observed shared/root/join/state/manager/quiz owner clusters with class-level role conversion. | Extract two-use similarities; create speculative primitives before converting remaining owners. | The Slice 5 inventory found zero qualifying cross-screen recipes. Extracting coincidence would add coupling without a demonstrated interface. | high | Avoids a new abstraction; future extraction remains easy when repetition appears. | aligned | accepted |
| D17 | Slice 6 / 2026-07-31 | accessibility / content invariant | The Room QR dialog needs scanner-stable contrast, and the final browser audit exposed a Radix missing-title/description warning. | Keep QR modules explicitly black on white in both dialects, treat that paper as content rather than chrome, and add screen-reader-only dialog title and description. | Theme QR colors; suppress the warning; leave the dialog unnamed. | Fixed black/white maximizes scanner reliability. The accessible name/description clears the Radix contract without changing visible UI; the retest logged no app errors. | high | Local, reversible, and improves interoperability/accessibility. | aligned | accepted |
