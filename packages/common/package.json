{
  "name": "@razzia/common",
  "type": "module",
  "scripts": {
    "types": "tsc --noEmit",
    "format": "prettier --check . --log-level warn",
    "format:fix": "prettier --write . --log-level warn",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "socket.io": "^4.8.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
